<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Base Dark Mode Theme -->
    <style name="Theme.ConnectMeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark_brown</item>  <!-- Dark Mode Primary Color -->
        <item name="colorPrimaryVariant">@color/dark_brown</item>
        <item name="colorSecondary">@color/dark_brown</item>

        <!-- ✅ Dark Mode Background Colors -->
        <item name="android:windowBackground">@color/dark_gray</item>
        <item name="android:colorBackground">@color/dark_gray</item>
        <item name="android:textColor">@color/white</item>

        <!-- ✅ Status & Navigation Bar Colors -->
        <item name="android:statusBarColor">@color/dark_gray</item>
        <item name="android:navigationBarColor">@color/dark_gray</item>
    </style>

</resources>
